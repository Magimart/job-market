import { describe, it } from "node:test";
import { useJobsStore } from "../../src/stores/jobs";


describe(("getters", ()=>{
    it("tests job api data types", ()=>{
        /*** complete test! */
        const isStore = useJobsStore()
    });
}))













